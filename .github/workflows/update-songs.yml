name: Update Songs JSON
on:
  push:
    paths:
      - 'songs/**'
jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Generate songs.json
        run: |
          # Script to auto-generate songs.json from uploaded files
          echo '[]' > songs.json
